gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# EXCEÇÃO deliberada (app de estudo): a chave release vive NO REPO —
# aura-release.jks + keystore.properties — para que todos os builds assinem
# igual e cada atualização instale sobre a anterior (ver app/build.gradle.kts).
!keystore.properties
!aura-release.jks
